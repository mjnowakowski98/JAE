class CommandDescriptor {
    constructor(_commandName, _constructorArgs) {
        let commandName = _commandName;
        let constructorargs = _constructorArgs;
    }
}

module.exports = CommandDescriptor;